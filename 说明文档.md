# LifeOS 项目说明文档

> 本文件为项目全生命周期唯一管理载体，包含**项目规划、实施方案、进度记录**三大模块。
> 每次重新打开项目前必须先阅读本文件确认进度；计划调整或进度变更时即时更新对应模块。

---

## 一、项目规划

| 项 | 内容 |
| --- | --- |
| 项目名称 | LifeOS V2.0 数字化生活助手 |
| 技术栈 | 后端 FastAPI + SQLAlchemy + MySQL 8；前端 React 19 + Vite + TS + Tailwind v4 |
| 后端端口 | 9515（`/api/v1`） |
| 前端端口 | 9015 |
| 部署形态 | Nginx 托管前端 dist，`/api/` 反向代理后端 |
| 当前阶段 | 功能迭代优化（品牌配置可配置化） |

### 规划中的待办（按优先级）

1. P0 系统品牌配置（LOGO / 标题）可配置化 —— 本轮执行
2. 后续优化项按用户反馈即时登记

---

## 二、实施方案

### 需求：系统 LOGO 与标题改为账户内可配置

**目标**：系统 LOGO、系统标题、登录页与注册页的 LOGO / 标题 / 副标题，全部由「系统设置」页面维护，取消底层硬编码。

**已确认的关键决策**

1. 配置作用域：**全局单例**（`site_config` 单行，id=1）。原因：登录/注册页为未登录状态，必须支持匿名读取。
2. LOGO 存储：**方案 A —— base64 data URL 存库**（`MEDIUMTEXT`）。原因：项目当前无静态文件托管，vite 仅代理 `/api`，nginx 仅转发 `/api/`；data URL 部署零改动。
3. 编辑权限：**仅管理员**（`is_admin=True`，首个注册用户自动成为管理员）。读取接口匿名公开。

**落点**

- 后端：`app/models/site_config.py`、`app/schemas/site_config.py`、`app/api/routes/site_config.py`、`core/security.py`（新增 `get_current_admin`）、`models/__init__.py`、`main.py`、`services/backup_service.py`（表中文名）、`middleware/activity_logger.py`（模块中文名）
- 前端：`src/lib/site-config.tsx`、`src/main.tsx`、`src/components/app-sidebar.tsx`、`src/layouts/admin-layout.tsx`、`src/pages/login.tsx`、`src/pages/register.tsx`、`src/pages/system/site-settings.tsx`、`src/config/navigation.ts`、`src/router.tsx`

**接口**

| 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- |
| GET | `/api/v1/site-config` | 匿名 | 返回归一化后的品牌配置 |
| PUT | `/api/v1/site-config` | 管理员 | 局部更新（空值回落默认） |
| POST | `/api/v1/site-config/reset` | 管理员 | 恢复全部内置默认 |

**约束**：LOGO 仅支持 PNG/JPEG/WEBP/SVG，原图 ≤200KB；SVG 内容做安全黑名单校验；标题 ≤64 字、副标题 ≤128 字。

**风险与对策**：首屏闪烁 → localStorage 快照 + `index.html` 默认值兜底；`TEXT` 列 64KB 限制 → 改用 `MEDIUMTEXT`；建表 → `create_all` 自动创建，无需迁移脚本（变更前先备份）。

---

## 三、进度记录

| 日期 | 任务 | 状态 | 结果说明 |
| --- | --- | --- | --- |
| 2026-09-24 | 系统品牌配置方案设计与确认 | 已完成 | 输出完整方案，确认全局单例 + data URL + 仅管理员可编辑 |
| 2026-09-24 | 创建本说明文档 | 已完成 | 建立项目规划 / 实施方案 / 进度记录三模块 |
| 2026-09-24 | P1 后端 site_config 实现 | 已完成 | 新增模型/Schema/路由，新增 `get_current_admin` 依赖，补齐备份表中文名与活动日志模块名 |
| 2026-09-24 | P2 前端 SiteConfigProvider | 已完成 | 新增全局品牌配置 Provider（localStorage 快照防闪烁）并接入 main.tsx |
| 2026-09-24 | P3 全站硬编码替换 | 已完成 | 侧栏 / admin-layout / 登录页 / 注册页改为读取全局配置 |
| 2026-09-24 | P4 系统设置页 | 已完成 | 新增 `/system-settings` 页面（表单 + 实时预览 + 恢复默认），接入导航与路由 |
| 2026-09-24 | P5 构建验证与提交 | 已完成 | 后端自测通过（匿名 GET 200、无令牌 PUT 401、LOGO 校验 4 例、`site_config` 表自动创建且 logo 为 MEDIUMTEXT）；前端 `vite build` 通过并推送 Git |
| 2026-09-24 | 修复既有 5 处类型检查错误 | 已完成 | `tsc -b` 全绿：bills 删除残留的 `selectedBill` 引用（删除账单会抛 ReferenceError）与未使用变量、饼图 `Cell` 改用官方 `shape` + `isActive` 实现悬停外扩（原 radius 写法被坐标覆盖，实际不生效）、清理 2 处未使用变量/导入 |
| 2026-09-24 | 更新 README 文档 | 已完成 | 补齐「系统设置 / 数据备份」功能条目、管理员机制说明、品牌配置相关安全与数据说明；修正不存在的 `migrations/` 目录描述与建表口径，补充 `说明文档.md` 与主题技术栈 |

---

## 四、遗留与注意

- `site_config` 为全局单例，修改会影响全站（含登录/注册页）；编辑权限仅限管理员账号。
- 备份导出/导入会包含品牌配置（含 LOGO base64），迁移时配置随备份走。
- 备份表中文名映射需在新增业务表时同步维护 `backup_service.TABLE_LABELS` 与 `activity_logger.MODULE_NAMES`。